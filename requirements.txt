# 象棋辅助工具依赖清单
# 安装: pip install -r requirements.txt

# -- 核心依赖 --
PySide6>=6.5.0           # Qt6 GUI (主界面+悬浮窗+托盘)
numpy>=1.24.0            # 数值计算

# -- 视觉识别 --
opencv-python>=4.8.0     # 图像处理 (棋盘定位+棋子识别)
Pillow>=10.0.0           # 截图 ImageGrab

# -- 引擎通信 (纯文本协议, 无额外依赖) --

# -- 可选依赖 --
# pyautogui>=0.9.0       # 鼠标模拟自动走棋 (可选)
# keyboard>=0.13.0       # 全局热键 (在图形界面下通常用Qt代替)
# py7zr>=0.20.0          # 解压引擎压缩包 (仅开发时使用)
